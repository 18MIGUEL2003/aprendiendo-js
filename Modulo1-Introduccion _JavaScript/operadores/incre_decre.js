/*
Ejemplos:

Incremento (++):
Incremento de prefijo (++variable):

let contador = 5; 
let resultado = ++contador; // contador es igual a 6, resultado es igual a 6

Incremento (++):
Incremento de sufijo (variable++):

let contador = 5;
let resultado = contador++; // contador es igual a 6, resultado es igual a 5

Decremento (--):
Decremento de prefijo (--variable):

let contador = 5; 
let resultado = --contador; // contador es igual a 4, resultado es igual a 4

Decremento (--):
Decremento de sufijo (variable--):

let contador = 5; 
let resultado = contador--; // contador es igual a 4, resultado es igual a 5

Los operadores de incremento y decremento son útiles para simplificar el código y realizar
operaciones de aumento o disminución de una unidad de manera rápida y eficiente.

Sin embargo, es importante tener en cuenta la diferencia entre las versiones de prefijo y
sufijo de estos operadores, ya que pueden afectar el resultado de las expresiones en las que se utilizan.

Ejemplo de uso de operadores de incremento en un bucle for:


for (let i = 0; i < 5; i++) {
  console.log(i); // Imprime los números del 0 al 4
}

En este ejemplo, se utiliza el operador de incremento de sufijo (i++) para aumentar el valor
de la variable i en cada iteración del bucle.


*/